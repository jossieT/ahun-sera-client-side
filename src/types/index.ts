export interface Service {
    id: string;
    name: string;
    description: string;
    icon: string;
}

export interface Tasker {
    id: string;
    name: string;
    profession: string;
    rating: number;
    hourlyRate: number;
}
